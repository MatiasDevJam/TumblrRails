<%= form_for([@post, @post.comments.build]) do |f| %>
    <p>
        <%= f.label :name %><br>
        <%= f.text_field :name %>
    </p>

      <p>
        <%= f.label :name %>
        <%= f.text_area :name %>
    </p>
    <br>
    <p>
        <%= f.submit %>
    </p>
<% end %>